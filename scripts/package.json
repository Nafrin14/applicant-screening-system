{
  "name": "mail-poller",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Standalone script run by .github/workflows/imap-poll.yml — fetches candidate email replies via IMAP and stores them in Supabase. Kept separate from the main app's package.json so CI installs stay fast.",
  "dependencies": {
    "@supabase/supabase-js": "^2.108.1",
    "imapflow": "^1.0.185",
    "mailparser": "^3.7.4"
  }
}
