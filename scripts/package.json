{
  "name": "mail-poller",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Standalone IMAP poller — runs continuously on Railway (loop mode) or once via GitHub Actions (--once flag).",
  "scripts": {
    "start": "node pollMail.js"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.108.1",
    "imapflow": "^1.0.185",
    "mailparser": "^3.7.4"
  }
}
